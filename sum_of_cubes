#include<stdio.h>
int main()
{
  int i,n,sum=0;
  printf("Enter the number of element\t");
  scanf("%d,&n);
  int j=1;
  for(i=1;i<=n;i++)
  {
    sum+=pow(j,3);
    printf("%d^3+",j);
  }
  printf("\b=%d\n\n",sum);
  return 0;
}
